#include <bits/stdc++.h>
using namespace std;

// Parenthesis Checker

int main()
{
    string str;
    cin >> str;
    bool check=false;

    // stack<string> stack;

    // for (int i = 0; i < str.length(); i++)
    // {
    //     stack.push(str[i]);
    // }

    for (int i = 0; i < str.length(); i++)
    {
        cout << str[i] << " ";
        if(){
            
        }
    }

    // while (!stack.empty())
    // {
    //     cout << " " << stack.top();
    //     stack.pop();
    // }

    return 0;
}